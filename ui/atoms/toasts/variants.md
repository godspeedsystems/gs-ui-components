```yaml
componentName: ToastMessage
filename: toast-message
category: toasts, notifications
layout: flex, alert
primaryUse: Displays a toast notification message with optional icon, action, and close button.
description: A flexible toast notification component supporting different types (success, error, info, warning), custom icons, actions, and dismiss functionality.
uses: notifications, alerts, feedback
dependencies: [react]
devDependencies: [typescript]
```

---

```yaml
componentName: ToastIcon
filename: toast-icon
category: toasts, notifications, icons
layout: inline, icon
primaryUse: Renders an icon corresponding to the toast type (success, error, info, warning).
description: A utility icon component for toast notifications, displaying different SVG icons and colors based on the notification type.
uses: notifications, alerts, feedback
dependencies: [react]
devDependencies: [typescript]
```

---
